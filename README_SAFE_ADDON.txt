INSIGHTS V9 SAFE ADD-ON

Radar Insights is a read-only view over radar.json. It does not write or regenerate radar data. It filters document debris and converts admitted radar material into concise topical signal statements. radar.json is intentionally omitted from this upload package to preserve the accumulated live corpus.
