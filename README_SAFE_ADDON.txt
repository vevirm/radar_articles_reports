FULL REPOSITORY — V15 SCAN REPAIR

1. Extract the ZIP locally.
2. Replace the repository contents with ALL extracted files and folders.
3. IMPORTANT: include the hidden .github folder. It contains the active workflow.
4. Commit everything to main.
5. Open GitHub Actions -> R&I Radar Scan -> Run workflow if you want to trigger it immediately.

The package contains the cumulative radar.json snapshot, scanner code, site, Insights page, configuration, tests and workflow.
No API key, custom secret or email configuration is required.
The scanner performs the four-month A/B backfill and then runs every 12 hours. A, B and C remain cumulative.
