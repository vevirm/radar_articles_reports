SCAN PAUSED — V17.12.1

While this file exists at the repository root, the GitHub Actions workflow
(.github/workflows/radar-scan.yml) skips the live scan on push, on schedule
and on manual dispatch. radar.json is NOT touched. The site is served as-is.

Purpose: verify that the supplied radar state (radar (23).json, 26 Aug 2026)
is inserted correctly and that all pages render correctly before any new
scan is allowed to change it.

TO RESUME SCANNING: delete this file and push. The next push, the next
12-hourly schedule, or a manual "Run workflow" will scan normally.
