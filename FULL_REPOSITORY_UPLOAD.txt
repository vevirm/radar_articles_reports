V17.4 COMPLETE REPOSITORY — GAP-PRIORITY + OPPORTUNITIES/RISKS EDITION

This ZIP is the WHOLE repository, including:
- the active GitHub Actions workflow;
- the cumulative/incremental scanner and persistent source cursors;
- the LIVE cumulative radar.json supplied for this build: 152 Strand A + 9 Strand B + 96 Strand C = 257 records;
- main Radar site;
- Radar Insights page;
- Sovereignty-Frontier Insight Summary and 4x4 matrix;
- new cumulative Greatest Opportunities & Risks page;
- exact matrix-coverage bridge used by the scanner to target sparse cells;
- tests.

Upload ALL files/folders to the SAME repository root, including .github.
Keep radar.json from this ZIP: it is the live accumulated corpus, not the old 8-item seed.

The upload commit to main immediately triggers the first scan. Bot-only radar.json commits do not retrigger the workflow.

The scanner also retains the older Git-history recovery safeguard for future seed-based repository upgrades.
