V17.2 COMPLETE REPOSITORY — PERSISTENT INCREMENTAL EDITION

This ZIP is the WHOLE repository, including:
- the active GitHub Actions workflow;
- scanner and configuration;
- cumulative radar.json seed/recovery logic;
- main radar site;
- Insights page;
- tests.

Upload ALL files/folders to the repository root, including .github.

The upload commit to main immediately triggers the first scan. Bot-only radar.json commits do not retrigger the workflow.
