V17.5.3 COMPLETE REPOSITORY — CURRENT CORPUS + ONE-TIME CORRECTIVE CLEANUP

This ZIP contains the whole repository, including the active workflow, scanner, pages, tests, and the exact current user-supplied radar.json: 72 A + 5 B + 101 C = 178 records.

FIRST RUN
1. Load the supplied current radar.json.
2. Run one corrective A/B precision audit (the earlier inherited audit is already complete).
3. Retain saved A/B that pass; refresh thin failures from DOI/page/PDF before deciding.
4. Run one corrective EU-first weak-signal audit of current Strand C.
5. Recompute Frontier coverage on cleaned A/B.
6. Run normal persistent rotations plus sparse-cell scholarly/institutional discovery and strict weak-signal recovery.
7. Save both corrective-complete markers.

EVERY LATER RUN
- skips all historical cleanup;
- screens new A/B and C candidates with the corrected gates;
- keeps persistent source rotations and gap priority.

Upload ALL files/folders to the same repository root, including .github. A commit to main starts the corrective cleanup + scan automatically.
