V17.5.5 DROP-IN UPDATE

1. Upload/replace the repository with the contents of this package.
2. Keep the bundled radar.json: it is the exact current radar (4).json supplied for this build (72 A / 5 B / 19 C).
3. Push to main to run the normal scanner workflow.

V17.5.5 does NOT repeat the historical cleanup. It preserves the current OpenAlex/Crossref/institution cursors, recomputes the 16 Frontier cells with balanced row-mechanism + directional semantic evidence, and adds scarcity-weighted discovery on top of normal rotation.

The Opportunities & Risks page shows variable-length ranked lists capped at 15 per side; no fixed six-item target remains.
