WHOLE REPOSITORY ONLY

Use the full ZIP supplied by ChatGPT. There is no separate workflow file to install.

1. Unzip the full repository once.
2. Upload/replace the repository contents in GitHub the same way you normally do.
3. Run the scanner.

The package contains the correct four-hour workflow. If GitHub web upload retains the older hidden workflow anyway, this release is backward-compatible: the tests will not block the scanner, and the legacy hourly/6-hour due gate is internally adjusted to an effective four-hour cadence.

Rules preserved:
- automatic scanner cadence: every 4 hours
- A/B/frontier: cumulative unless explicitly cleaned as false positive/duplicate/integrity failure
- Strand C: expires 60 days after first_seen
- scanner has no stored repository credential while scanning
- all non-radar.json changes are removed before push credentials exist
- only radar.json is staged/committed
