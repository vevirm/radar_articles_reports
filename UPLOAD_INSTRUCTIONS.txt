V17.3 COMPLETE REPOSITORY — SIMPLE UPLOAD

1. Extract the ZIP.
2. Upload/replace ALL files and folders in the GitHub repository root.
3. Make sure the hidden .github folder is included.
4. Commit the upload directly to main.

THAT COMMIT STARTS THE FIRST SCAN AUTOMATICALLY.
You do not need to press "Run workflow" unless GitHub Actions does not start after the commit.

Afterwards the workflow runs automatically every 12 hours.

What V17.3 adds:
- a new /frontier/ Insight Summary page for Sovereignty-Frontier Signals;
- the 4 × 4 independence–competitiveness matrix;
- strict "pay attention" triage above the broad cumulative radar;
- no external AI API and no new database.

What remains protected:
- the V17.2 persistent OpenAlex/Crossref/institution cursors;
- the cumulative A/B/C corpus;
- whole-repository recovery from Git history;
- the 30-minute GitHub job and 20-minute scanner budget;
- the existing main Radar and Radar Insights pages.

Do not delete radar.json before upload. The bundled file has a one-run seed marker that can recover a larger pre-upload corpus from Git history on the first scan.
