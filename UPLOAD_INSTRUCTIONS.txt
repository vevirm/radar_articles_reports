v17.20.50: upload the complete repository. Existing OPENALEX_API_KEY Actions secret is reused automatically. Page HTML/CSS/reader logic is unchanged; this release resets only the active corpus/state and scanner-side curation/discovery machinery.

Release: 17.20.50

UPLOAD THE WHOLE REPOSITORY
===========================

This ZIP is a COMPLETE repository snapshot, not a patch.

1. Unzip the package.
2. Open the `radar_articles_reports-main` folder.
3. Upload/replace the COMPLETE contents of that folder on top of the existing GitHub repository.
4. Let files with the same names replace the older versions.

IMPORTANT: STALE .github WORKFLOWS NO LONGER BLOCK THE SCANNER
-------------------------------------------------------------
GitHub browser uploads have repeatedly left `.github/workflows/*.yml` at an older revision.
v17.20.48 remains deliberately compatible with that situation. The scanner regression suite no
longer exits non-zero merely because hidden workflow YAML is stale. Visible Python runtime
guards handle the legacy hourly/six-hour Main workflow and prevent Main/Historical overlap.

The package still contains the preferred corrected workflows. If GitHub accepts them, Main
runs on the fixed four-hour schedule and Main/Historical share one concurrency group. If
GitHub leaves the old YAML in place, the upload-triggered Main scan still proceeds.


V17.20.48 SCANNER CLEANUP
-------------------------
This release keeps the existing radar corpus and reader pages, but retires migration-era
scanner load: normal scholarly discovery is incremental, priority-journal depth rotates in
a small bounded lane, DOI-backed citation snowballing rotates through new seed neighborhoods,
and low-yield generic rescue no longer reserves 600 seconds of every run.

OPENALEX API KEY
----------------
Do NOT place the OpenAlex key in this repository or in any file in the ZIP. In GitHub, add it
as an Actions repository secret named exactly `OPENALEX_API_KEY` under:
Settings -> Secrets and variables -> Actions. The workflows already read that secret.

Do not cherry-pick individual files. Use the whole repository package.
