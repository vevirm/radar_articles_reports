v17.19.15 — WHOLE REPOSITORY ONLY

1. Unzip this full repository package.
2. Upload/replace the repository contents as you normally do.
3. Run the scanner once manually.

There is NO separate workflow file to install.

This release specifically handles the case where GitHub web upload keeps the older hidden .github workflow. The old workflow safety check is not disabled: the scanner now tells it explicitly when a high-confidence precision cleanup happened, so intentional false-positive removal is allowed while unexpected corpus loss still fails closed.

The package contains the latest generated radar state, preserves scan cursors, keeps A/B/frontier cumulative, keeps Strand C for 60 days from first_seen, and preserves the no-credential-during-scan / radar.json-only commit boundary.
