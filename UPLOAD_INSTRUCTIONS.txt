R&I GEOPOLITICS RADAR — FULL REPOSITORY UPLOAD
UPLOAD-READY EDITION: 85 FILES TOTAL

This version is specifically compacted to stay below GitHub's 100-file web-upload limit.
You still upload the complete repository; there is no patching or merging.

1. Unzip this bundle on your computer.
2. Open your GitHub repository and choose Add file > Upload files.
3. Drag the ENTIRE unzipped folder into the upload area so its subfolders are included too.
4. Upload all 85 files as one complete repository replacement.
5. Make sure the .github folder is included. It contains the automated scanners.
6. Keep the existing GitHub Actions secret OPENALEX_API_KEY configured.
7. Commit the upload to main.

WHY THERE ARE FEWER FILES:
The original package had dozens of individual Python regression-test files plus a bundled test archive.
This upload-ready edition stores the 38 main regression tests inside tests/all_tests.zip and keeps
one loader, tests/test_all.py. GitHub Actions still runs that same test bundle normally.

DO NOT unzip tests/all_tests.zip. It belongs inside the repository as one file.
