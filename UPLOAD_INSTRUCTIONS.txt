R&I GEOPOLITICS RADAR V17.11.0 — EXACT-LINK REVIEWED MANUAL EVIDENCE

NORMAL RADAR BUILD
1. Serve the repository root and open index.html.
2. Run scripts/scan_radar.py only for a real live scan.

MANUAL CANDIDATE LIST
  python scripts/manual_ingest.py PATH_TO_LIST --state radar.json

Supported: DOCX, PDF, CSV, JSON, YAML/YML, TXT, Markdown.
Use --links-validated when curator-supplied URLs have been tested.
Use --review-evidence REVIEW.json only for reviewed evidence bound to the exact supplied record URLs.
Use --no-fetch for comparison/registration or when the reviewed cache supplies the underlying source evidence.

IMPORTANT
- Manual entries are not automatic admissions.
- Manual ingestion does not use search-engine discovery; it targets supplied URLs.
- Curator matrix cells are hypotheses, not forced placements.
- Metadata-only records defer.
- Manual ingest preserves live-scan timestamps/cursors/history.
