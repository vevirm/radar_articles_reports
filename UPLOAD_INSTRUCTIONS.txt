V17.5.6 DROP-IN UPDATE

1. Upload/replace the repository with the contents of this package.
2. Keep the bundled radar.json. It is byte-for-byte the current GitHub JSON supplied for this repair (84 A / 5 B / 19 C).
3. Push to main to run the normal scanner workflow.

Preservation guarantees:
- no historical cleanup is re-triggered;
- incremental_state_version stays v17.2-persistent-source-cursors;
- source_expansion_version stays v17.5.2-gap-report-recall;
- the existing OpenAlex/Crossref/institution/Frontier cursors and fingerprints are retained;
- new per-cell rotation and result-depth state is added lazily on the first scan.

The scanner now rotates sparse-cell query variants and specialist sources persistently and checks deeper OpenAlex/Crossref result pages while still checking page 1 for newly published material.
