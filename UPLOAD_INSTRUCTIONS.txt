R&I GEOPOLITICS RADAR — FULL REPOSITORY

1. Replace the repository with the complete contents of this bundle. Do not merge this bundle over an old file tree: GitHub keeps files that are absent from an upload, which can leave stale tests or an old hidden workflow active.
2. Keep repository secret OPENALEX_API_KEY configured in GitHub Actions.
3. Main Radar is scheduled every four hours at minute 17 and uses the full 24-minute research allocation: if ordinary queues finish early, remaining time is rotated into different healthy source/query lanes rather than ending the scan.
4. Historical is scheduled every four hours at minute 57, 40 minutes after Main, and uses a 10-minute budget.
5. Main and Historical share one concurrency slot. They never research simultaneously. Main has priority; Historical waits or is pre-empted if necessary.
6. Main A/B is cumulative. The original 200 is a baseline, not a cap.
7. Strand C is temporary and relative to A: every C signal requires a substantive Strand-A anchor and expires 60 days after first_seen.
8. Historical accepts only publications strictly older than the rolling six-month Main boundary and does not create Strand C.
9. The production workflows do not run old repository-history regression tests before the scanner.
10. Browser pages have no repository write credential. Only radar.json (Main) or historical/historical.json (Historical) may survive a scanner run and be committed after validation.
11. A whole-repository upload may trigger Main immediately. If GitHub retains an older hidden Historical workflow, you may also briefly see a Historical action on upload; compatibility code makes that upload-triggered Historical run metadata-only with zero source requests, so Main remains the only research scan. Scheduled Historical research remains separate and 10 minutes.
