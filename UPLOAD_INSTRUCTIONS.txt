V17.2 COMPLETE REPOSITORY — SIMPLE UPLOAD

1. Extract the ZIP.
2. Upload/replace ALL files and folders in the GitHub repository root.
3. Make sure the hidden .github folder is included.
4. Commit the upload directly to main.

THAT COMMIT STARTS THE FIRST SCAN AUTOMATICALLY.
You do not need to press "Run workflow" unless GitHub Actions does not start after the commit.

Afterwards the workflow runs automatically every 12 hours.

What is different in V17.2:
- it remembers independent cursors for OpenAlex, Crossref and institutions;
- it does not restart from the first scholarly/report query every run;
- it scans only a bounded rotating batch from each large source family;
- known DOI/title/link identities are discarded early;
- all previously accepted site items remain cumulative;
- the four-month backfill is completed progressively across several safe runs;
- weak signals still run every 12 hours;
- the Insights page is included and reads the same cumulative radar.json.

Do not delete radar.json before upload. The bundled file has a one-run seed marker that can recover a larger pre-upload corpus from Git history on the first scan.
