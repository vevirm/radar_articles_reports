V17.3 — V17.2 INCREMENTAL SCANNER RETAINED

V17.3 adds the Sovereignty-Frontier Insight Summary but deliberately does NOT replace the scanner that is already working.

The GitHub job remains 30 minutes and the scanner remains capped at 20 minutes. Persistent source-family cursors remain stored in radar.json, so scheduled runs continue instead of restarting the same large source universe.

Key values remain:
- .github/workflows/radar-scan.yml: timeout-minutes: 30
- radar_config.json: scan_budget_seconds: 1200
- radar_config.json: network_reserve_seconds: 150
- OpenAlex: 40 rotating queries/run
- Crossref broad: 35 rotating queries/run
- Crossref priority: 45 rotating tasks/run
- institutions: 18 rotating sources/run

The upload commit to main starts the first scan immediately. Normal schedule: every 12 hours.

Version: 17.3
