V17.5.1 PRESERVATION — START FROM THE USER-SUPPLIED LIVE CORPUS

The radar.json in this ZIP is the original live file supplied for this build:
- Strand A: 156 records
- Strand B: 10 records
- Strand C: 100 records
- Total: 266 accumulated records

Do not replace it with an old seed.

On the FIRST V17.5.1 run only, the scanner audits inherited Strand A/B before discovery. Records that already pass the new gate are retained. A record that fails on its shortened saved summary gets one best-effort DOI/page/PDF refresh before the final keep/remove decision. Strand C is preserved. The completed run writes inherited_corpus_audit_complete=true.

After that first run, retained historical A/B material is not re-audited again. Every NEW candidate must pass the corrected boundary-aware R&I + geopolitics/economic-security gate.

The scanner then recomputes the exact 4x4 Frontier coverage on the cleaned corpus, prioritises the emptiest/sparsest cells (including knowledge-D brain drain/talent outflow), and continues the normal persistent OpenAlex/Crossref/institution rotations.

UPLOAD SAFELY
1. Extract the complete ZIP.
2. Upload/replace ALL files and folders at the root of the SAME GitHub repository, including .github.
3. Keep radar.json from this ZIP.
4. Commit directly to main.
5. The first run performs audit + scan. Later scheduled runs scan only.
