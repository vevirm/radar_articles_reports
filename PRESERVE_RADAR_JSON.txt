NO MANUAL PRESERVATION STEP IS REQUIRED IN V17.

The complete repository ZIP includes radar.json. It is marked as an upload seed so the first scan can recover/merge a larger cumulative radar from Git history before revalidation and scanning.
