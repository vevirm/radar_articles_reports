# Stage 5B — worked-example parity and chain-own-record distance

This shadow-only patch tightens the claim reasoning before any detector switch.

- Distance and the not-already-said check now exclude the candidate chain's own records (R-10/R-11).
- Dependency commitments may inherit stake significance from explicit secondary objects, allowing the EuroHPC public-procurement claim to anchor the AI-factory pathway without retagging the primary object.
- Dependency capability matching uses the commitment object's primary cluster, not secondary-cluster tags, preventing strategic-goal tags from creating spurious bridges.
- Conflicting criteria may join distinct endpoint objects through an exact-object frontier; they are no longer forced onto one primary object.
- A small semantic repair aligns the corpus with the worked compute and quantum examples (Finland/Google/EuroHPC/quantum/export-control and bridging records).
- Publication remains locked: no shadow candidate is publishable without the later falsifier ledger and oddity/watchability gates.

Local acceptance: protected-core freeze passes; reasoning-reform smoke suite 104/104 passes.
