# Stage 5 final — complete claim-native shadow

This stage completes the claim-native shadow implementation without switching any live detector or reader output.

What is now in shadow:

- R-10 cluster distance with candidate-chain records excluded.
- R-31 exact-object / secondary-object frontier expansion.
- Level 2 `corroborated_claim`.
- Level 3 sequence/gap shapes: `practice_before_doctrine`, `deployment_before_rules`, `clock_before_rule`, `goal_without_measure`, `stalled_proposal`, `success_metric_gap`.
- Level 4 `opposing_movements`.
- Level 5 `dependency_pathway`, `conflicting_criteria`, `latent_channel`, `anchor_demand`, `split_recurrence`.
- Level 3 `era_conjunction` using current-vs-historical object-pair lift.
- A count-based legacy-vs-claim shadow diff in every artifact.

Safety remains unchanged:

- scanner is not modified;
- Deep Scan decisions are not modified;
- `radar.json`, `radar_active.json`, reader files and publication state are not written by the shadow;
- every shadow Level-5 publication gate remains locked until falsifier execution is wired by candidate fingerprint;
- detector switching remains disabled.

17 September 2026 acceptance snapshot:

- protected-core freeze: pass;
- reasoning-reform smoke suite: 115/115 pass;
- claim semantic quality gate: ready;
- current shadow score-gate passes: one, the worked quantum testing-infrastructure × export-control-competence chain (89); publication gate: false;
- compute public-procurement → energy → permitting remains a watch pathway (78), matching the worked arithmetic;
- clock-before-rule finds the public Gigafactory deadline with only pending/intention/delivered permitting-and-power rules;
- success-metric-gap isolates talent retention vs recruitment rather than cross-domain objective noise;
- latent-channel outputs remain watch-only where the explicit live connection is missing;
- current corpus does not yet supply qualifying `anchor_demand`, `split_recurrence`, or `era_conjunction` outputs under the conservative claim-only gates; the grammars are implemented and covered by synthetic fixtures.

The shadow workflow now also runs automatically after successful full/quick Radar scans and Deep Scan imports, while retaining manual `workflow_dispatch`. This provides the required observation period without allowing the shadow to publish.
