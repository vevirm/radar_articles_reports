V17.7.5 ROTATING CELL FILL

Build goal: make empty Frontier cells genuinely rotate through recovery formulations across runs and preserve source evidence needed for cell classification.

Key changes:
- dedicated persisted `frontier_recovery_query_cursors` per Frontier cell
- V17.7.4 migration seeds those cursors from saved `scholarly:<cell>` cursors
- stubborn recovery round-robins all currently empty cells instead of slicing the same first 30-query bank every run
- recovery formulation cursors advance only when a request actually executes
- matrix-depth cursor advances only across the contiguous executed prefix of a wave
- OpenAlex/Crossref gap-query provenance is carried internally on admitted candidates
- three-sentence evidence summaries preserve a real target-mechanism sentence when present in the source abstract
- final published matrix occupancy is recomputed and recorded after A/C/evidence merge
- current bundled state is `radar (16).json`
- full suite: 204 tests pass
