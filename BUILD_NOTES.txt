R&I Geopolitics Radar V17.12.1 — presentation-first repair

Purpose of this package:
- make the first repository upload render the already-bundled radar state immediately;
- do not spend ~20 minutes scanning just to find a frontend problem;
- start the real scanner manually only after the Pages presentation has been inspected.

Changes:
- fixed the Insight Summary page-level undefined clean() runtime failure;
- split radar.json load failures from presentation/render failures;
- changed .github/workflows/radar-scan.yml to workflow_dispatch only;
- added a short push-time presentation smoke check with no network discovery;
- preserved the supplied radar.json byte-for-byte.

No live scan was run while producing this package.

Presentation refinement:
- added read/ subpage based on the supplied “Read This If Nothing Else” concept;
- simplified it into three disclosure depths: claim -> reasoning -> evidence;
- made “Read this first” the first action on the main page;
- no scanner or radar state changes.
