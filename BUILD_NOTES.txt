V17.5.8 KEYLESS DISCOVERY HARDENING

- Preserves the supplied radar.json without migration/reset.
- Removes all OpenAlex API-key/GitHub-secret integration.
- OpenAlex is explicitly keyless and fails fast on 429/allowance exhaustion.
- Retains date-bounded OpenAlex newest + persisted depth rotation.
- Crossref uses relevance discovery plus newest/deeper lanes, with from/until publication dates and future-date rejection.
- Retains persisted Frontier query/source rotation.
- Persists source warning details in radar.json on new scans.
- Current live state refreshed from radar (6).json after successful scans: 93 A / 5 B / 19 C; no previously accepted items removed.
- Preservation regression tests now enforce minimum historical baselines rather than a frozen exact corpus count, so successful future discovery does not fail tests.
