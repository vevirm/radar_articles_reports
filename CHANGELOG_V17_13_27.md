# V17.13.27 — Matrix reasoning + language hierarchy

## Matrix logic

- Carries forward the V17.13.26 audit of all 16 cells.
- Every placement must show the row mechanism, the control/dependence direction and the performance direction in source-supported evidence.
- A requires an observed/committed capability gain, B a real control-for-cost trade-off, C productive outside reliance, and D a concrete loss/constraint.
- Generic terms such as `foreign`, `risk`, `capacity`, `investment`, `market` or `access` do not establish a cell.
- Sparse cells receive more discovery effort but never relaxed admission/classification rules.
- The current production classifier passes all 31 semantic regression fixtures.

## Reader-language hierarchy

- **Read at least this:** simplest wording (`readText`).
- **Matrix + Risks & opportunities:** simple analytical wording (`matrixText`), intentionally simpler than the main Radar.
- **Main Radar + Evidence browser:** policy-technical wording (`radarText`); the main Radar prefers the validated stored core message before plain-language fallback.
- **Excel:** most technical layer, including source-merit components, admission/review fields and Matrix diagnostics.
- Future scanner saves persist the V17.13.27 presentation/language profile metadata from `radar_config.json`.

## Matrix presentation

- Public row labels: People & knowledge; Tools & facilities; Firms & growth; Rules & decisions.
- Public column labels: More control, stronger; More control, some cost; Stronger, but reliant; Less control, weaker.
- Public cell names are direct outcome phrases such as Build and keep talent, Useful outside access and Value and scale move abroad.
- Full-Matrix confidence/screening/quadrant diagnostics are secondary under `Technical placement check`.
- Risks & opportunities keeps the cleaner ranked-card design and uses the same simple analytical language.

## Read presentation

- Live issue labels are authored in ordinary language instead of being simplified twice.
- Removed awkward labels such as `Rules, standards & rules`, `Chips & chips` and `Economic ability to compete`.
- Source-strength wording stays simple (`Top source`, `Strong source`, etc.).

## Technical workbook

- `stuff/source_merit_ranking.xlsx` is rebuilt as a four-sheet technical evidence workbook:
  1. Ranked sources
  2. Technical evidence
  3. Matrix criteria
  4. Method
- The workbook contains 216 deduplicated current evidence records and documents the 4-month core / Highest-to-6-month exception.

## Corpus/window rules

- Bundled evidence remains 186 A / 24 B / 14 C.
- Matrix currently places 38 findings under the stricter semantic contract.
- English-publication-only A/B, disabled inference-only external bridges, scheduler catch-up/preservation hard stops, and the 4-month core / Highest-to-6-month policy are unchanged.
- No discovery scan was run for this presentation/classifier release.
